# Test Environment Configuration
# This database is separate from development - tests will reset it

PORT=3001
HOST=0.0.0.0
NODE_ENV=test

# Test database - use a separate database to avoid conflicts with dev
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/scoops_test?schema=public"
