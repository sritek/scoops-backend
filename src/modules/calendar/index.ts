/**
 * Calendar Module
 *
 * Manages academic calendar events (holidays, PTMs, exams, deadlines, etc.)
 */

export { calendarRoutes } from "./calendar.routes";
export * as calendarService from "./calendar.service.js";
